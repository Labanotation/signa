<div class="position-relative overflow-hidden p-2 p-md-2 m-md-2 text-center bg-light">
  <div class="card col-md-4 p-lg-1 mx-auto my-1">
    <div class="card-body text-left">
      <h5 class="card-title text-center">{{{__ "signup.title"}}}</h5>
      {{#if validated}}
        <div class="alert alert-success text-center">
          <p>Votre inscription à l'application Signa a bien été validée, merci et bienvenue !</p>
          <p>Vous pouvez dès à présent vous connecter et créer votre premier projet...</p>
          <a class="btn btn-outline-secondary bg-light" href="/login">{{{__ "login.submit"}}}</a>
        </div>
      {{else}}
        {{#if error}}
          <div class="alert alert-danger text-center">
            <p>Une erreur est survenue lors de votre demande d'inscription à l'application Signa.</p>
            <p>Merci de bien vouloir essayer à nouveau.</p>
            <a class="btn btn-outline-secondary bg-light" href="/signup">{{{__ "signup.submit"}}}</a>
          </div>
        {{else}}
          {{#if pending}}
          <div class="alert alert-success text-center">
            <p>Votre demande d'inscription à l'application Signa a bien été prise en compte.</p>
            <p>Vous allez recevoir rapidement un e-mail vous permettant de valider celle-ci.</p>
          </div>
          <div class="alert alert-warning text-center">
            <p>Attention : vous ne disposez que d'une heure pour activer le lien de validation !</p>
          </div>
          {{else}}
          <form action="/signup" method="post">
            <div class="form-group">
              <label for="email">{{{__ "signup.email"}}}</label>
              <input type="text" class="form-control {{#if mail_invalid}}is-invalid{{/if}}" name="email" id="email"
                autocomplete="email" value="{{mail_value}}" required>
              <div class="invalid-feedback">
                {{{__ "signup.mail_invalid" }}}
              </div>
              <small id="emailHelpBlock" class="form-text text-muted">
                {{{__ "signup.mail_help" }}}
              </small>
            </div>
            <div class="form-group">
              <label for="login">{{{__ "login.login"}}}</label>
              <input type="text" class="form-control {{#if login_invalid}}is-invalid{{/if}}" name="login" id="login"
                autocomplete="username" value="{{login_value}}" required>
              <input type="text" class="form-control-fullname" name="fullname" id="fullname" value="">
              <div class="invalid-feedback">
                {{{__ "signup.login_invalid" }}}
              </div>
              <small id="loginHelpBlock" class="form-text text-muted">
                {{{__ "signup.login_help" }}}
              </small>
            </div>
            <div class="form-group">
              <label for="password">{{{__ "login.password"}}}</label>
              <input type="password" class="form-control {{#if password_invalid}}is-invalid{{/if}}" name="password"
                autocomplete="new-password" id="password" value="{{password_value}}" required>
              <div class="invalid-feedback">
                {{{__ "signup.password_invalid" }}}
              </div>
              <small id="passwordHelpBlock" class="form-text text-muted">
                {{{__ "signup.password_help" }}}
              </small>
            </div>
            <div class="form-group">
              <label for="confirm_password">{{{__ "signup.confirm_password"}}}</label>
              <input type="password" class="form-control {{#if password_different}}is-invalid{{/if}}" name="confirm_password"
                id="confirm_password" value="{{confirm_password_value}}" required>
              <div class="invalid-feedback">
                {{{__ "signup.password_different" }}}
              </div>
            </div>
            <div class="btn-toolbar justify-content-end">
              <button type="submit" class="btn btn-primary my-2">{{{__ "signup.submit"}}}</button>
            </div>
          </form>
          {{/if}}
        {{/if}}
      {{/if}}
    </div>
  </div>
</div>

{{#contentFor "pageStyles"}}
<style type="text/css">
  .form-control-fullname {
    display: none;
  }
</style>
{{/contentFor}}