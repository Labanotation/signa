<div class="position-relative overflow-hidden p-3 p-md-4 m-md-3 text-center bg-light">
  <div class="card col-md-4 p-lg-1 mx-auto my-1">
    <div class="card-body text-left">
      <h5 class="card-title text-center">{{{__ "forgotpassword.title"}}}</h5>
      {{#if pending}}
      <div class="alert alert-success text-center">
        <p>Votre demande de réinitialisation du mot de passe a bien été prise en compte.</p>
        <p>Vous allez recevoir rapidement un e-mail à l'adresse <pre>{{pending}}</pre> afin d'effectuer celle-ci.</p>
      </div>
      <div class="alert alert-warning text-center">
        <p>Attention : vous ne disposez que d'une heure pour activer le lien de réinitialisation !</p>
      </div>
      {{else}}
      <form action="/forgotpassword" method="post">
        <div class="form-group">
          <label for="login">{{{__ "login.login"}}}</label>
          <input type="text" class="form-control {{#if invalid}}is-invalid{{/if}}" name="login" id="login"
            autocomplete="username" value="{{login_value}}" required>
          <input type="text" class="form-control-fullname" name="fullname" id="fullname" value="">
          <div class="invalid-feedback">
            {{{__ "forgotpassword.invalid" }}}
          </div>
          <small id="loginHelpBlock" class="form-text text-muted">
            {{{__ "forgotpassword.login_help" }}}
          </small>
          <small id="forgotpasswordHelpBlock" class="form-text text-muted text-right">
            <a class="nav-link" href="/forgotlogin">{{{__ "forgotpassword.forgotlogin"}}}</a>
          </small>
        </div>
        <div class="btn-toolbar justify-content-between">
          <button type="submit" class="btn btn-primary my-2">{{{__ "forgotpassword.submit"}}}</button>
        </div>
      </form>
      {{/if}}
    </div>
  </div>
</div>

{{#contentFor "pageStyles"}}
<style type="text/css">
  .form-control-fullname {
    display: none;
  }
</style>
{{/contentFor}}